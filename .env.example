# The current Stripe Checkout integration only requires the secret key at runtime.
# The publishable key can stay here for future client-side Stripe features if needed.
PORT=4242
# Used for Stripe redirect URLs after successful payment.
BASE_URL=http://127.0.0.1:5500
PUBLISHABLE_KEY=pk_test_replace_me
SECRET_KEY=sk_test_replace_me
